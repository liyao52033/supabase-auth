{
    "name": "supabase-auth-api",
    "version": "1.0.0",
    "description": "Authentication API with Supabase for Tencent EdgeOne Pages",
    "scripts": {
        "init": "edgeone pages init",
        "deploy": "rm -rf .edgeone && edgeone pages deploy -n supabase"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.39.0"
    }
}
